JOB_TYPE = "dataspatial_georeference"


def georeference_datastore_table(
    resource: str,
    api_key: str,
    result_url: str,
    site_url: str,
    job_created: str,
) -> None:
    # todo - call georeferencing functions and provide status updates
    pass
